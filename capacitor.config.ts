import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourco.shopifyappforall',
  appName: 'ShopifyAppForAll',
  webDir: 'dist'
};

export default config;
